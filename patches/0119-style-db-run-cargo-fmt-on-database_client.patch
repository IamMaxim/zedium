From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Maxim Stepanov <g.maxim.stepanoff@gmail.com>
Date: Wed, 1 Jul 2026 23:44:36 +0300
Subject: [PATCH] style(db): run cargo fmt on database_client

---
 .../database_client/src/connection/client.rs  |  25 ++--
 .../src/connection/introspect.rs              |  25 +++-
 .../database_client/src/connection/profile.rs |  15 ++-
 .../database_client/src/connection/store.rs   |  12 +-
 .../database_client/src/connection_modal.rs   |  26 ++--
 crates/database_client/src/database_client.rs |   2 +-
 crates/database_client/src/panel.rs           | 123 ++++++++----------
 crates/database_client/src/persistence.rs     |   3 +-
 crates/database_client/src/query_view.rs      |  89 ++++++++-----
 9 files changed, 171 insertions(+), 149 deletions(-)

diff --git a/crates/database_client/src/connection/client.rs b/crates/database_client/src/connection/client.rs
index 5aa13e2d84..8b9fc88adb 100644
--- a/crates/database_client/src/connection/client.rs
+++ b/crates/database_client/src/connection/client.rs
@@ -42,10 +42,8 @@ impl Connection {
                 }
                 SslMode::Prefer | SslMode::Require => {
                     let tls = make_rustls_connector();
-                    let (client, connection) = config
-                        .connect(tls)
-                        .await
-                        .context("connect (tls)")?;
+                    let (client, connection) =
+                        config.connect(tls).await.context("connect (tls)")?;
                     tokio::spawn(async move {
                         if let Err(error) = connection.await {
                             log::error!("database connection error: {error}");
@@ -123,9 +121,10 @@ pub(crate) fn collect_result(
 #[cfg(test)]
 fn profile_password_from_url(url: &str) -> Option<String> {
     use std::str::FromStr as _;
-    tokio_postgres::Config::from_str(url)
-        .ok()
-        .and_then(|cfg| cfg.get_password().and_then(|p| String::from_utf8(p.to_vec()).ok()))
+    tokio_postgres::Config::from_str(url).ok().and_then(|cfg| {
+        cfg.get_password()
+            .and_then(|p| String::from_utf8(p.to_vec()).ok())
+    })
 }
 
 #[cfg(test)]
@@ -193,9 +192,7 @@ mod tests {
             .await
             .unwrap();
         let result = cx
-            .update(|cx| {
-                conn.execute("select 1 as one, 'x' as label where false".into(), 500, cx)
-            })
+            .update(|cx| conn.execute("select 1 as one, 'x' as label where false".into(), 500, cx))
             .await
             .unwrap();
         assert_eq!(result.columns, vec!["one".to_string(), "label".to_string()]);
@@ -219,13 +216,7 @@ mod tests {
             .await
             .unwrap();
         let result = cx
-            .update(|cx| {
-                conn.execute(
-                    "select 1 as a where false; select 2 as b".into(),
-                    500,
-                    cx,
-                )
-            })
+            .update(|cx| conn.execute("select 1 as a where false; select 2 as b".into(), 500, cx))
             .await
             .unwrap();
         assert_eq!(result.columns, vec!["b".to_string()]);
diff --git a/crates/database_client/src/connection/introspect.rs b/crates/database_client/src/connection/introspect.rs
index 4390e54ce7..ba31f5f9ee 100644
--- a/crates/database_client/src/connection/introspect.rs
+++ b/crates/database_client/src/connection/introspect.rs
@@ -121,7 +121,12 @@ impl Connection {
         cx.background_spawn(async move { Ok(parse_relations(&task.await?)) })
     }
 
-    pub fn list_columns(&self, schema: String, table: String, cx: &App) -> Task<Result<Vec<ColumnInfo>>> {
+    pub fn list_columns(
+        &self,
+        schema: String,
+        table: String,
+        cx: &App,
+    ) -> Task<Result<Vec<ColumnInfo>>> {
         let sql = format!(
             "select column_name, data_type from information_schema.columns \
              where table_schema = {} and table_name = {} order by ordinal_position",
@@ -222,9 +227,10 @@ mod tests {
         cx.executor().allow_parking();
         cx.update(|cx| gpui_tokio::init(cx));
         let profile = crate::connection::profile::ConnectionProfile::from_url(&url).unwrap();
-        let password = tokio_postgres::Config::from_str(&url)
-            .ok()
-            .and_then(|cfg| cfg.get_password().and_then(|p| String::from_utf8(p.to_vec()).ok()));
+        let password = tokio_postgres::Config::from_str(&url).ok().and_then(|cfg| {
+            cfg.get_password()
+                .and_then(|p| String::from_utf8(p.to_vec()).ok())
+        });
         Some(
             cx.update(|cx| Connection::connect(profile, password, cx))
                 .await
@@ -240,7 +246,10 @@ mod tests {
         };
         let schemas = cx.update(|cx| conn.list_schemas(cx)).await.unwrap();
         let names: Vec<_> = schemas.iter().map(|s| s.name.as_str()).collect();
-        assert!(names.contains(&"public"), "expected public schema, got {names:?}");
+        assert!(
+            names.contains(&"public"),
+            "expected public schema, got {names:?}"
+        );
         assert!(names.contains(&"app"), "expected app schema, got {names:?}");
         assert!(
             !names.iter().any(|n| n.starts_with("pg_")),
@@ -273,8 +282,10 @@ mod tests {
             .update(|cx| conn.list_relations("public".to_string(), cx))
             .await
             .unwrap();
-        let by_name: std::collections::HashMap<_, _> =
-            relations.iter().map(|r| (r.name.as_str(), r.kind)).collect();
+        let by_name: std::collections::HashMap<_, _> = relations
+            .iter()
+            .map(|r| (r.name.as_str(), r.kind))
+            .collect();
         assert_eq!(by_name.get("users"), Some(&RelationKind::Table));
         assert_eq!(by_name.get("orders"), Some(&RelationKind::Table));
         assert_eq!(by_name.get("user_emails"), Some(&RelationKind::View));
diff --git a/crates/database_client/src/connection/profile.rs b/crates/database_client/src/connection/profile.rs
index e0998639e2..f9a46948a3 100644
--- a/crates/database_client/src/connection/profile.rs
+++ b/crates/database_client/src/connection/profile.rs
@@ -112,7 +112,10 @@ mod tests {
 
     #[test]
     fn ssl_mode_serializes_lowercase() {
-        assert_eq!(serde_json::to_string(&SslMode::Require).unwrap(), "\"require\"");
+        assert_eq!(
+            serde_json::to_string(&SslMode::Require).unwrap(),
+            "\"require\""
+        );
     }
 
     #[test]
@@ -135,8 +138,14 @@ mod tests {
     fn pg_config_sets_ssl_mode() {
         let mut p = sample();
         p.ssl_mode = SslMode::Require;
-        assert_eq!(p.pg_config(None).get_ssl_mode(), tokio_postgres::config::SslMode::Require);
+        assert_eq!(
+            p.pg_config(None).get_ssl_mode(),
+            tokio_postgres::config::SslMode::Require
+        );
         p.ssl_mode = SslMode::Disable;
-        assert_eq!(p.pg_config(None).get_ssl_mode(), tokio_postgres::config::SslMode::Disable);
+        assert_eq!(
+            p.pg_config(None).get_ssl_mode(),
+            tokio_postgres::config::SslMode::Disable
+        );
     }
 }
diff --git a/crates/database_client/src/connection/store.rs b/crates/database_client/src/connection/store.rs
index ae698eeac7..6829609c70 100644
--- a/crates/database_client/src/connection/store.rs
+++ b/crates/database_client/src/connection/store.rs
@@ -4,7 +4,9 @@ use gpui::{App, AppContext as _, Task};
 use std::path::{Path, PathBuf};
 
 pub fn connections_path() -> PathBuf {
-    paths::config_dir().join("database_client").join("connections.json")
+    paths::config_dir()
+        .join("database_client")
+        .join("connections.json")
 }
 
 pub fn load_profiles() -> Vec<ConnectionProfile> {
@@ -69,8 +71,12 @@ mod tests {
 
     fn sample() -> ConnectionProfile {
         ConnectionProfile {
-            id: "p1".into(), name: "Local".into(), host: "localhost".into(),
-            port: 5432, database: "mydb".into(), user: "alice".into(),
+            id: "p1".into(),
+            name: "Local".into(),
+            host: "localhost".into(),
+            port: 5432,
+            database: "mydb".into(),
+            user: "alice".into(),
             ssl_mode: SslMode::Prefer,
         }
     }
diff --git a/crates/database_client/src/connection_modal.rs b/crates/database_client/src/connection_modal.rs
index ad2499c52c..4bd6691c1b 100644
--- a/crates/database_client/src/connection_modal.rs
+++ b/crates/database_client/src/connection_modal.rs
@@ -26,11 +26,7 @@ pub struct ConnectionModal {
 }
 
 impl ConnectionModal {
-    pub fn new(
-        panel: Entity<DatabasePanel>,
-        window: &mut Window,
-        cx: &mut Context<Self>,
-    ) -> Self {
+    pub fn new(panel: Entity<DatabasePanel>, window: &mut Window, cx: &mut Context<Self>) -> Self {
         let name_input = cx.new(|cx| {
             let mut editor = Editor::single_line(window, cx);
             editor.set_placeholder_text("Connection name", window, cx);
@@ -189,8 +185,12 @@ impl ConnectionModal {
             let mut profiles = store::load_profiles();
             profiles.push(profile.clone());
             match store::save_profiles(&profiles) {
-                Ok(()) => { store::store_password(cx, &profile, &password).detach_and_log_err(cx); }
-                Err(e) => { log::error!("failed to save connection profile: {e}"); }
+                Ok(()) => {
+                    store::store_password(cx, &profile, &password).detach_and_log_err(cx);
+                }
+                Err(e) => {
+                    log::error!("failed to save connection profile: {e}");
+                }
             }
         }
 
@@ -267,13 +267,11 @@ impl Render for ConnectionModal {
                     .child(self.user_input.clone())
                     .child(Label::new("Password"))
                     .child(self.password_input.clone())
-                    .child(
-                        Button::new("ssl-mode", ssl_label).on_click(cx.listener(
-                            |this, _, window, cx| {
-                                this.cycle_ssl_mode(window, cx);
-                            },
-                        )),
-                    ),
+                    .child(Button::new("ssl-mode", ssl_label).on_click(cx.listener(
+                        |this, _, window, cx| {
+                            this.cycle_ssl_mode(window, cx);
+                        },
+                    ))),
             )
     }
 }
diff --git a/crates/database_client/src/database_client.rs b/crates/database_client/src/database_client.rs
index d152b53467..d32211367a 100644
--- a/crates/database_client/src/database_client.rs
+++ b/crates/database_client/src/database_client.rs
@@ -6,9 +6,9 @@ use workspace::{Workspace, register_serializable_item};
 mod connection;
 mod connection_modal;
 mod panel;
+mod persistence;
 pub mod query_view;
 mod tree;
-mod persistence;
 
 pub use connection::client::DEFAULT_RESULT_LIMIT;
 pub use panel::DatabasePanel;
diff --git a/crates/database_client/src/panel.rs b/crates/database_client/src/panel.rs
index 7a6b353203..f233959269 100644
--- a/crates/database_client/src/panel.rs
+++ b/crates/database_client/src/panel.rs
@@ -2,12 +2,7 @@ use std::collections::{HashMap, HashSet};
 
 use crate::{
     ToggleFocus,
-    connection::{
-        client::Connection,
-        introspect::RelationKind,
-        profile::ConnectionProfile,
-        store,
-    },
+    connection::{client::Connection, introspect::RelationKind, profile::ConnectionProfile, store},
     tree::{NodeKind, TreeState},
 };
 use anyhow::Result;
@@ -196,7 +191,8 @@ impl DatabasePanel {
                     // Same server, different database: clone the profile onto it.
                     let mut profile = profile.expect("checked above");
                     profile.database = db_name.clone();
-                    cx.update(|cx| Connection::connect(profile, password, cx)).await
+                    cx.update(|cx| Connection::connect(profile, password, cx))
+                        .await
                 }
             };
 
@@ -415,12 +411,7 @@ impl DatabasePanel {
         cx.notify();
     }
 
-    fn edit_connection(
-        &mut self,
-        profile_id: String,
-        window: &mut Window,
-        cx: &mut Context<Self>,
-    ) {
+    fn edit_connection(&mut self, profile_id: String, window: &mut Window, cx: &mut Context<Self>) {
         let Some(profile) = self.profiles.iter().find(|p| p.id == profile_id).cloned() else {
             return;
         };
@@ -449,39 +440,33 @@ impl DatabasePanel {
         let entity = cx.entity();
 
         let context_menu = ContextMenu::build(window, cx, |menu, _, _| {
-            menu.item(
-                ContextMenuEntry::new("Disconnect").handler({
-                    let entity = entity.clone();
-                    let profile_id = profile_id.clone();
-                    move |_window, cx| {
-                        entity.update(cx, |this, cx| {
-                            this.disconnect_connection(profile_id.clone(), cx);
-                        });
-                    }
-                }),
-            )
-            .item(
-                ContextMenuEntry::new("Edit").handler({
-                    let entity = entity.clone();
-                    let profile_id = profile_id.clone();
-                    move |window, cx| {
-                        entity.update(cx, |this, cx| {
-                            this.edit_connection(profile_id.clone(), window, cx);
-                        });
-                    }
-                }),
-            )
-            .item(
-                ContextMenuEntry::new("Delete").handler({
-                    let entity = entity.clone();
-                    let profile_id = profile_id.clone();
-                    move |_window, cx| {
-                        entity.update(cx, |this, cx| {
-                            this.delete_connection(profile_id.clone(), cx);
-                        });
-                    }
-                }),
-            )
+            menu.item(ContextMenuEntry::new("Disconnect").handler({
+                let entity = entity.clone();
+                let profile_id = profile_id.clone();
+                move |_window, cx| {
+                    entity.update(cx, |this, cx| {
+                        this.disconnect_connection(profile_id.clone(), cx);
+                    });
+                }
+            }))
+            .item(ContextMenuEntry::new("Edit").handler({
+                let entity = entity.clone();
+                let profile_id = profile_id.clone();
+                move |window, cx| {
+                    entity.update(cx, |this, cx| {
+                        this.edit_connection(profile_id.clone(), window, cx);
+                    });
+                }
+            }))
+            .item(ContextMenuEntry::new("Delete").handler({
+                let entity = entity.clone();
+                let profile_id = profile_id.clone();
+                move |_window, cx| {
+                    entity.update(cx, |this, cx| {
+                        this.delete_connection(profile_id.clone(), cx);
+                    });
+                }
+            }))
         });
 
         window.focus(&context_menu.focus_handle(cx), cx);
@@ -598,15 +583,15 @@ impl Render for DatabasePanel {
                 .gap_1()
                 .when(has_children, |this| {
                     this.child(
-                        Disclosure::new(("disclosure", index), is_expanded).on_click(
-                            cx.listener(move |this, _, _, cx| {
+                        Disclosure::new(("disclosure", index), is_expanded).on_click(cx.listener(
+                            move |this, _, _, cx| {
                                 this.toggle_node(
                                     node_id_for_toggle.clone(),
                                     kind_for_toggle.clone(),
                                     cx,
                                 );
-                            }),
-                        ),
+                            },
+                        )),
                     )
                 })
                 .when(!has_children, |this| this.pl(px(20.0)))
@@ -626,17 +611,11 @@ impl Render for DatabasePanel {
                     )
                 })
                 .when(is_relation, |this| {
-                    this.on_click(cx.listener(
-                        move |this, event: &ClickEvent, window, cx| {
-                            if event.click_count() >= 2 {
-                                this.open_query_for_relation(
-                                    &node_id_for_query,
-                                    window,
-                                    cx,
-                                );
-                            }
-                        },
-                    ))
+                    this.on_click(cx.listener(move |this, event: &ClickEvent, window, cx| {
+                        if event.click_count() >= 2 {
+                            this.open_query_for_relation(&node_id_for_query, window, cx);
+                        }
+                    }))
                 })
                 .when(is_connection, |this| {
                     this.on_mouse_down(
@@ -682,11 +661,11 @@ impl Render for DatabasePanel {
                         .color(Color::Muted),
                 )
                 .child(
-                    Button::new("add-connection-empty", "Add connection").on_click(
-                        cx.listener(|this, _, window, cx| {
+                    Button::new("add-connection-empty", "Add connection").on_click(cx.listener(
+                        |this, _, window, cx| {
                             this.open_new_connection_modal(window, cx);
-                        }),
-                    ),
+                        },
+                    )),
                 )
                 .into_any_element()
         } else {
@@ -768,7 +747,9 @@ mod tests {
             if panel.read_with(cx, |panel, _| predicate(panel)) {
                 return;
             }
-            cx.background_executor.timer(Duration::from_millis(25)).await;
+            cx.background_executor
+                .timer(Duration::from_millis(25))
+                .await;
         }
         let labels = panel.read_with(cx, |panel, _| {
             panel
@@ -897,7 +878,10 @@ mod tests {
             keys.sort();
             keys
         });
-        assert_eq!(conn_keys, vec!["p1/emptydb".to_string(), "p1/testdb".to_string()]);
+        assert_eq!(
+            conn_keys,
+            vec!["p1/emptydb".to_string(), "p1/testdb".to_string()]
+        );
         // Empty schema shows the explicit placeholder.
         let empty_present = panel.read_with(cx, |panel, _| {
             panel
@@ -906,6 +890,9 @@ mod tests {
                 .iter()
                 .any(|n| n.kind == NodeKind::Empty && n.label == "(no tables)")
         });
-        assert!(empty_present, "expected '(no tables)' placeholder under emptydb/public");
+        assert!(
+            empty_present,
+            "expected '(no tables)' placeholder under emptydb/public"
+        );
     }
 }
diff --git a/crates/database_client/src/persistence.rs b/crates/database_client/src/persistence.rs
index 3848549e96..a4b22646c8 100644
--- a/crates/database_client/src/persistence.rs
+++ b/crates/database_client/src/persistence.rs
@@ -35,7 +35,8 @@ impl QueryDb {
         sql: String,
     ) -> Result<()> {
         self.write(move |conn| {
-            let query_str = "INSERT INTO database_query_views (item_id, workspace_id, profile_id, sql)
+            let query_str =
+                "INSERT INTO database_query_views (item_id, workspace_id, profile_id, sql)
                 VALUES (?1, ?2, ?3, ?4)
                 ON CONFLICT (item_id, workspace_id) DO UPDATE SET
                     profile_id = excluded.profile_id,
diff --git a/crates/database_client/src/query_view.rs b/crates/database_client/src/query_view.rs
index 54e49da286..bc4d8500d7 100644
--- a/crates/database_client/src/query_view.rs
+++ b/crates/database_client/src/query_view.rs
@@ -8,12 +8,12 @@ use gpui::{
 };
 use language::Buffer;
 use project::Project;
-use util::ResultExt as _;
 use settings::Settings as _;
 use theme::ActiveTheme;
 use theme_settings::ThemeSettings;
-use ui::{Table, TableInteractionState};
 use ui::prelude::*;
+use ui::{Table, TableInteractionState};
+use util::ResultExt as _;
 use workspace::{
     ItemId, SerializableItem, Workspace, WorkspaceId, delete_unloaded_items,
     item::{Item, ItemEvent},
@@ -96,12 +96,16 @@ impl QueryView {
             .detach();
         }
 
-        let editor_subscription = cx.subscribe(&editor, |this, _editor, event: &EditorEvent, cx| {
-            if matches!(event, EditorEvent::BufferEdited | EditorEvent::Edited { .. }) {
-                this.needs_serialize = true;
-                cx.emit(QueryViewEvent::Edit);
-            }
-        });
+        let editor_subscription =
+            cx.subscribe(&editor, |this, _editor, event: &EditorEvent, cx| {
+                if matches!(
+                    event,
+                    EditorEvent::BufferEdited | EditorEvent::Edited { .. }
+                ) {
+                    this.needs_serialize = true;
+                    cx.emit(QueryViewEvent::Edit);
+                }
+            });
 
         QueryView {
             editor,
@@ -134,7 +138,15 @@ impl QueryView {
         let workspace_id = workspace.database_id();
         let label = label.map(SharedString::from);
         let view = cx.new(|cx| {
-            let mut view = QueryView::new(profile, connection, initial_sql, weak, workspace_id, window, cx);
+            let mut view = QueryView::new(
+                profile,
+                connection,
+                initial_sql,
+                weak,
+                workspace_id,
+                window,
+                cx,
+            );
             view.label = label;
             view
         });
@@ -145,9 +157,8 @@ impl QueryView {
         let sql = self.editor.read(cx).text(cx);
 
         if self.connection.is_none() && self.profile.is_none() {
-            self.error = Some(
-                "No connection — open this query from a table in the Database panel".into(),
-            );
+            self.error =
+                Some("No connection — open this query from a table in the Database panel".into());
             cx.notify();
             return;
         }
@@ -270,7 +281,11 @@ impl Item for QueryView {
     fn tab_content_text(&self, _detail: usize, _cx: &App) -> SharedString {
         self.label
             .clone()
-            .or_else(|| self.profile.as_ref().map(|p| SharedString::from(p.name.clone())))
+            .or_else(|| {
+                self.profile
+                    .as_ref()
+                    .map(|p| SharedString::from(p.name.clone()))
+            })
             .unwrap_or_else(|| "Query".into())
     }
 
@@ -344,7 +359,15 @@ impl SerializableItem for QueryView {
                 .find(|p| p.id == profile_id);
             cx.update(|window, cx| {
                 Ok(cx.new(|cx| {
-                    QueryView::new(profile, None, sql, workspace.clone(), Some(workspace_id), window, cx)
+                    QueryView::new(
+                        profile,
+                        None,
+                        sql,
+                        workspace.clone(),
+                        Some(workspace_id),
+                        window,
+                        cx,
+                    )
                 }))
             })?
         })
@@ -441,27 +464,23 @@ impl Render for QueryView {
 
                 let table = Table::new(cols)
                     .header(columns)
-                    .uniform_list(
-                        "query-results",
-                        rows.len(),
-                        move |range, _window, _cx| {
-                            range
-                                .map(|i| {
-                                    let Some(row) = rows.get(i) else {
-                                        return Vec::new();
-                                    };
-                                    row.iter()
-                                        .map(|cell| match cell {
-                                            None => Label::new("NULL")
-                                                .color(Color::Muted)
-                                                .into_any_element(),
-                                            Some(s) => Label::new(s.clone()).into_any_element(),
-                                        })
-                                        .collect::<Vec<AnyElement>>()
-                                })
-                                .collect()
-                        },
-                    )
+                    .uniform_list("query-results", rows.len(), move |range, _window, _cx| {
+                        range
+                            .map(|i| {
+                                let Some(row) = rows.get(i) else {
+                                    return Vec::new();
+                                };
+                                row.iter()
+                                    .map(|cell| match cell {
+                                        None => Label::new("NULL")
+                                            .color(Color::Muted)
+                                            .into_any_element(),
+                                        Some(s) => Label::new(s.clone()).into_any_element(),
+                                    })
+                                    .collect::<Vec<AnyElement>>()
+                            })
+                            .collect()
+                    })
                     .empty_table_callback(|_window, _cx| {
                         Label::new("No rows returned.")
                             .size(LabelSize::Small)
