From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Maxim Stepanov <g.maxim.stepanoff@gmail.com>
Date: Thu, 2 Jul 2026 11:14:11 +0300
Subject: [PATCH] feat(db): render query results through a resizable
 ResultsGrid with row-number gutter and results font

Derives Clone on QueryResult so QueryView can hand the grid its own copy
while keeping the toolbar row-count/truncation labels.
---
 .../database_client/src/connection/client.rs  |   1 +
 crates/database_client/src/database_client.rs |   1 +
 crates/database_client/src/query_view.rs      |  47 +-
 crates/database_client/src/results_grid.rs    | 451 +++++++++++++++++-
 4 files changed, 463 insertions(+), 37 deletions(-)

diff --git a/crates/database_client/src/connection/client.rs b/crates/database_client/src/connection/client.rs
index 4aa6f922b8..82a0c96542 100644
--- a/crates/database_client/src/connection/client.rs
+++ b/crates/database_client/src/connection/client.rs
@@ -7,6 +7,7 @@ use std::sync::Arc;
 
 pub const DEFAULT_RESULT_LIMIT: usize = 500;
 
+#[derive(Clone)]
 pub struct QueryResult {
     pub columns: Vec<String>,
     pub rows: Vec<Vec<Option<String>>>,
diff --git a/crates/database_client/src/database_client.rs b/crates/database_client/src/database_client.rs
index f4935b06ca..cd6cc68c64 100644
--- a/crates/database_client/src/database_client.rs
+++ b/crates/database_client/src/database_client.rs
@@ -35,6 +35,7 @@ actions!(
 /// Initialize the database client: register actions, panel, and items.
 pub fn init(cx: &mut App) {
     query_view::register_keybindings(cx);
+    results_grid::register_keybindings(cx);
     panel::register_keybindings(cx);
     register_serializable_item::<query_view::QueryView>(cx);
     cx.observe_new(|workspace: &mut Workspace, _, _| {
diff --git a/crates/database_client/src/query_view.rs b/crates/database_client/src/query_view.rs
index 51b220b360..8611cd3cb4 100644
--- a/crates/database_client/src/query_view.rs
+++ b/crates/database_client/src/query_view.rs
@@ -12,7 +12,6 @@ use settings::Settings as _;
 use theme::ActiveTheme;
 use theme_settings::ThemeSettings;
 use ui::prelude::*;
-use ui::{Table, TableInteractionState};
 use util::ResultExt as _;
 use workspace::{
     ItemId, SerializableItem, Workspace, WorkspaceId, delete_unloaded_items,
@@ -27,6 +26,7 @@ use crate::{
         store,
     },
     persistence::QueryDb,
+    results_grid::ResultsGrid,
 };
 
 actions!(database_client, [RunQuery]);
@@ -59,7 +59,7 @@ pub struct QueryView {
     needs_serialize: bool,
     /// Optional short label shown in the tab title (e.g. the table name for double-click opens).
     label: Option<SharedString>,
-    table_interaction: Entity<TableInteractionState>,
+    results_grid: Entity<ResultsGrid>,
     // Retained for future actions that need workspace access (e.g. opening linked tabs).
     #[allow(dead_code)]
     workspace: WeakEntity<Workspace>,
@@ -79,7 +79,7 @@ impl QueryView {
     ) -> Self {
         let buffer = cx.new(|cx| Buffer::local(initial_sql, cx));
         let editor = cx.new(|cx| Editor::for_buffer(buffer.clone(), None, window, cx));
-        let table_interaction = cx.new(|cx| TableInteractionState::new(cx));
+        let results_grid = cx.new(ResultsGrid::new);
 
         cx.spawn({
             let workspace = workspace.clone();
@@ -124,7 +124,7 @@ impl QueryView {
             running: false,
             needs_serialize: false,
             label: None,
-            table_interaction,
+            results_grid,
             workspace,
             workspace_id,
             _editor_subscription: editor_subscription,
@@ -180,6 +180,7 @@ impl QueryView {
         let start = Instant::now();
         let existing_connection = self.connection.clone();
         let profile = self.profile.clone();
+        let sortable = crate::sql_paging::wrappable(&sql);
 
         cx.spawn(async move |this, cx| {
             // Use the live connection if present, otherwise lazily connect using the stored
@@ -242,6 +243,9 @@ impl QueryView {
                 }
                 match outcome {
                     Ok(result) => {
+                        this.results_grid.update(cx, |grid, cx| {
+                            grid.set_result(Some(result.clone()), sortable, cx);
+                        });
                         this.result = Some(result);
                         this.error = None;
                         this.elapsed_ms = Some(elapsed);
@@ -249,6 +253,8 @@ impl QueryView {
                         cx.notify();
                     }
                     Err(err) => {
+                        this.results_grid
+                            .update(cx, |grid, cx| grid.set_result(None, false, cx));
                         this.error = Some(err.to_string());
                         this.result = None;
                         this.elapsed_ms = Some(elapsed);
@@ -499,42 +505,11 @@ impl Render for QueryView {
                     )
                     .into_any_element()
             } else {
-                let columns = result.columns.clone();
-                let rows = result.rows.clone();
-                let cols = columns.len();
-
-                let table = Table::new(cols)
-                    .header(columns)
-                    .uniform_list("query-results", rows.len(), move |range, _window, _cx| {
-                        range
-                            .map(|i| {
-                                let Some(row) = rows.get(i) else {
-                                    return Vec::new();
-                                };
-                                row.iter()
-                                    .map(|cell| match cell {
-                                        None => Label::new("NULL")
-                                            .color(Color::Muted)
-                                            .into_any_element(),
-                                        Some(s) => Label::new(s.clone()).into_any_element(),
-                                    })
-                                    .collect::<Vec<AnyElement>>()
-                            })
-                            .collect()
-                    })
-                    .empty_table_callback(|_window, _cx| {
-                        Label::new("No rows returned.")
-                            .size(LabelSize::Small)
-                            .color(Color::Muted)
-                            .into_any_element()
-                    })
-                    .interactable(&self.table_interaction);
-
                 div()
                     .flex_1()
                     .w_full()
                     .overflow_hidden()
-                    .child(table)
+                    .child(self.results_grid.clone())
                     .into_any_element()
             }
         } else {
diff --git a/crates/database_client/src/results_grid.rs b/crates/database_client/src/results_grid.rs
index a0a8c25bce..ffca359a6b 100644
--- a/crates/database_client/src/results_grid.rs
+++ b/crates/database_client/src/results_grid.rs
@@ -1,10 +1,39 @@
 //! DataGrip-style results grid: resizable columns, row-number gutter,
 //! cell selection/copy, and a cell context menu.
 
-use gpui::SharedString;
+use gpui::{
+    AbsoluteLength, App, ClickEvent, ClipboardItem, Context, DismissEvent, Entity, EventEmitter,
+    Focusable as _, FontWeight, KeyBinding, MouseButton, MouseDownEvent, Pixels, Point, Render,
+    SharedString, Subscription, Window, actions, anchored, deferred, px,
+};
+use settings::Settings as _;
+use theme::ActiveTheme;
+use theme_settings::ThemeSettings;
+use ui::prelude::*;
+use ui::{
+    ColumnWidthConfig, ContextMenu, ContextMenuEntry, ResizableColumnsState, Table,
+    TableInteractionState, TableResizeBehavior,
+};
 
+use crate::connection::client::QueryResult;
+use crate::settings::DatabaseClientSettings;
 use crate::sql_paging::SortDirection;
 
+actions!(
+    database_client,
+    [
+        /// Copies the selected results-grid cell's raw value to the clipboard.
+        CopyCellValue
+    ]
+);
+
+pub(crate) fn register_keybindings(cx: &mut App) {
+    cx.bind_keys([
+        KeyBinding::new("cmd-c", CopyCellValue, Some("DatabaseResultsGrid")),
+        KeyBinding::new("ctrl-c", CopyCellValue, Some("DatabaseResultsGrid")),
+    ]);
+}
+
 /// Approximate advance width of one glyph as a fraction of the font size.
 /// The results font defaults to the (monospace) buffer font, where ~0.6 is typical.
 const CHAR_WIDTH_FACTOR: f32 = 0.62;
@@ -85,6 +114,390 @@ pub(crate) fn row_to_json(columns: &[String], row: &[Option<String>]) -> String
     serde_json::Value::Object(object).to_string()
 }
 
+/// Events emitted by [`ResultsGrid`] for its owning `QueryView` to handle.
+pub enum ResultsGridEvent {
+    /// User clicked a sortable header. The grid has already applied the
+    /// none→asc→desc→none cycling decision ([`next_sort`]); the payload is the
+    /// requested new sort state (column name + direction), or `None` to clear
+    /// sorting. The owner stores it verbatim, re-queries, then calls
+    /// [`ResultsGrid::set_result`] + [`ResultsGrid::set_sort`].
+    SortRequested(Option<(SharedString, SortDirection)>),
+}
+
+/// DataGrip-style grid over one [`QueryResult`]: pinned row-number gutter,
+/// resizable columns, cell selection/copy, and a cell context menu.
+pub struct ResultsGrid {
+    interaction: Entity<TableInteractionState>,
+    columns_state: Option<Entity<ResizableColumnsState>>,
+    result: Option<QueryResult>,
+    numeric_columns: Vec<bool>,
+    /// (row index, data-column index) — the gutter is not selectable.
+    selected_cell: Option<(usize, usize)>,
+    sortable: bool,
+    sort: Option<(SharedString, SortDirection)>,
+    context_menu: Option<(Entity<ContextMenu>, Point<Pixels>, Subscription)>,
+}
+
+impl EventEmitter<ResultsGridEvent> for ResultsGrid {}
+
+impl ResultsGrid {
+    pub fn new(cx: &mut Context<Self>) -> Self {
+        ResultsGrid {
+            interaction: cx.new(|cx| TableInteractionState::new(cx)),
+            columns_state: None,
+            result: None,
+            numeric_columns: Vec::new(),
+            selected_cell: None,
+            sortable: false,
+            sort: None,
+            context_menu: None,
+        }
+    }
+
+    /// Replaces the displayed result. Recomputes numeric columns and (re)builds
+    /// the column-widths state from the new data. Clears selection/context menu.
+    /// `sortable`: headers clickable (caller passes `sql_paging::wrappable(&sql)`).
+    pub fn set_result(
+        &mut self,
+        result: Option<QueryResult>,
+        sortable: bool,
+        cx: &mut Context<Self>,
+    ) {
+        self.selected_cell = None;
+        self.context_menu = None;
+        self.sortable = sortable;
+        match &result {
+            Some(result) if !result.columns.is_empty() => {
+                self.numeric_columns = detect_numeric_columns(result.columns.len(), &result.rows);
+                let font_size = f32::from(Self::results_font_size(cx));
+                let data_widths = estimate_column_widths(&result.columns, &result.rows, font_size);
+                let gutter = row_number_column_width(result.rows.len(), font_size);
+                let mut initial_widths: Vec<AbsoluteLength> =
+                    Vec::with_capacity(data_widths.len() + 1);
+                initial_widths.push(AbsoluteLength::Pixels(px(gutter)));
+                initial_widths.extend(
+                    data_widths
+                        .iter()
+                        .map(|width| AbsoluteLength::Pixels(px(*width))),
+                );
+                let mut behaviors = vec![TableResizeBehavior::None];
+                behaviors.extend(std::iter::repeat_n(
+                    // MinSize is in rems: 60px at the default 16px-per-rem.
+                    TableResizeBehavior::MinSize(MIN_COLUMN_WIDTH / 16.0),
+                    data_widths.len(),
+                ));
+                let cols = data_widths.len() + 1;
+                self.columns_state =
+                    Some(cx.new(|_| ResizableColumnsState::new(cols, initial_widths, behaviors)));
+            }
+            _ => {
+                self.numeric_columns.clear();
+                self.columns_state = None;
+            }
+        }
+        self.result = result;
+        cx.notify();
+    }
+
+    /// Confirmed sort state to display as a header chevron (set by the owner
+    /// after the sorted page arrives).
+    pub fn set_sort(
+        &mut self,
+        sort: Option<(SharedString, SortDirection)>,
+        cx: &mut Context<Self>,
+    ) {
+        self.sort = sort;
+        cx.notify();
+    }
+
+    fn results_font_size(cx: &App) -> Pixels {
+        DatabaseClientSettings::get_global(cx).resolved_results_font_size(cx)
+    }
+
+    fn results_font_family(cx: &App) -> SharedString {
+        DatabaseClientSettings::get_global(cx).resolved_results_font_family(cx)
+    }
+
+    fn cycle_sort(&mut self, column: SharedString, cx: &mut Context<Self>) {
+        cx.emit(ResultsGridEvent::SortRequested(next_sort(
+            self.sort.as_ref(),
+            &column,
+        )));
+    }
+
+    fn select_cell(&mut self, row: usize, col: usize, window: &mut Window, cx: &mut Context<Self>) {
+        self.selected_cell = Some((row, col));
+        let focus_handle = self.interaction.read(cx).focus_handle.clone();
+        window.focus(&focus_handle, cx);
+        cx.notify();
+    }
+
+    fn copy_selected_cell(&mut self, _cx: &mut Context<Self>) {
+        // Implemented in the selection/copy step.
+    }
+
+    fn deploy_cell_context_menu(
+        &mut self,
+        row: usize,
+        col: usize,
+        position: Point<Pixels>,
+        window: &mut Window,
+        cx: &mut Context<Self>,
+    ) {
+        let Some(result) = &self.result else {
+            return;
+        };
+        let Some(row_values) = result.rows.get(row).cloned() else {
+            return;
+        };
+        let Some(column_name) = result.columns.get(col).cloned() else {
+            return;
+        };
+        let cell_value = row_values.get(col).cloned().flatten().unwrap_or_default();
+        let row_tsv = row_to_tsv(&row_values);
+        let row_json = row_to_json(&result.columns, &row_values);
+
+        let context_menu = ContextMenu::build(window, cx, |menu, _, _| {
+            menu.item(
+                ContextMenuEntry::new("Copy Value").handler(move |_window, cx| {
+                    cx.write_to_clipboard(ClipboardItem::new_string(cell_value.clone()));
+                }),
+            )
+            .item(
+                ContextMenuEntry::new("Copy Row").handler(move |_window, cx| {
+                    cx.write_to_clipboard(ClipboardItem::new_string(row_tsv.clone()));
+                }),
+            )
+            .item(
+                ContextMenuEntry::new("Copy Row as JSON").handler(move |_window, cx| {
+                    cx.write_to_clipboard(ClipboardItem::new_string(row_json.clone()));
+                }),
+            )
+            .item(
+                ContextMenuEntry::new("Copy Column Name").handler(move |_window, cx| {
+                    cx.write_to_clipboard(ClipboardItem::new_string(column_name.clone()));
+                }),
+            )
+        });
+
+        window.focus(&context_menu.focus_handle(cx), cx);
+        let subscription = cx.subscribe(&context_menu, |this, _, _: &DismissEvent, cx| {
+            this.context_menu.take();
+            cx.notify();
+        });
+        self.context_menu = Some((context_menu, position, subscription));
+        cx.notify();
+    }
+
+    fn render_cell(
+        &self,
+        row_ix: usize,
+        col_ix: usize,
+        cell: &Option<String>,
+        row_height: Pixels,
+        selected: Option<(usize, usize)>,
+        numeric: &[bool],
+        cx: &mut Context<Self>,
+    ) -> AnyElement {
+        let is_selected = selected == Some((row_ix, col_ix));
+        let is_null = cell.is_none();
+        let text: SharedString = match cell {
+            Some(value) => SharedString::from(value.clone()),
+            None => "NULL".into(),
+        };
+        div()
+            .id(("db-cell", row_ix * numeric.len().max(1) + col_ix))
+            .h(row_height)
+            .w_full()
+            .px_1p5()
+            .whitespace_nowrap()
+            .text_ellipsis()
+            .overflow_hidden()
+            .when(numeric.get(col_ix).copied().unwrap_or(false), |this| {
+                this.text_right()
+            })
+            .when(is_null, |this| {
+                this.italic().text_color(cx.theme().colors().text_muted)
+            })
+            .when(is_selected, |this| {
+                this.bg(cx.theme().colors().element_selected)
+                    .border_1()
+                    .border_color(cx.theme().colors().border_focused)
+            })
+            .on_mouse_down(
+                MouseButton::Left,
+                cx.listener(move |this, _: &MouseDownEvent, window, cx| {
+                    this.select_cell(row_ix, col_ix, window, cx);
+                }),
+            )
+            .on_mouse_down(
+                MouseButton::Right,
+                cx.listener(move |this, event: &MouseDownEvent, window, cx| {
+                    this.select_cell(row_ix, col_ix, window, cx);
+                    this.deploy_cell_context_menu(row_ix, col_ix, event.position, window, cx);
+                }),
+            )
+            .child(text)
+            .into_any_element()
+    }
+}
+
+impl Render for ResultsGrid {
+    fn render(&mut self, _window: &mut Window, cx: &mut Context<Self>) -> impl IntoElement {
+        let (Some(result), Some(columns_state)) = (&self.result, &self.columns_state) else {
+            return div().size_full().into_any_element();
+        };
+        let data_cols = result.columns.len();
+        let cols = data_cols + 1;
+        let row_count = result.rows.len();
+
+        let theme_settings = ThemeSettings::get_global(cx);
+        let font_family = Self::results_font_family(cx);
+        let font_size = Self::results_font_size(cx);
+        let row_height: Pixels = font_size * theme_settings.ui_line_height();
+        // py_0p5 (2px top + bottom) + the 1px bottom border of the header row.
+        let header_height = row_height + px(5.);
+
+        let text_muted = cx.theme().colors().text_muted;
+        let rows = result.rows.clone();
+        let numeric = self.numeric_columns.clone();
+        let selected = self.selected_cell;
+
+        let mut header_cells: Vec<AnyElement> = Vec::with_capacity(cols);
+        header_cells.push(div().into_any_element()); // gutter header
+        for (col, name) in result.columns.iter().enumerate() {
+            let name = SharedString::from(name.clone());
+            let sort_icon = self
+                .sort
+                .as_ref()
+                .filter(|(sorted, _)| *sorted == name)
+                .map(|(_, direction)| match direction {
+                    SortDirection::Ascending => IconName::ChevronUp,
+                    SortDirection::Descending => IconName::ChevronDown,
+                });
+            header_cells.push(
+                div()
+                    .id(("db-header", col))
+                    .w_full()
+                    .flex()
+                    .flex_row()
+                    .items_center()
+                    .gap_1()
+                    .font_weight(FontWeight::MEDIUM)
+                    .child(
+                        div()
+                            .flex_1()
+                            .whitespace_nowrap()
+                            .text_ellipsis()
+                            .overflow_hidden()
+                            .child(name.clone()),
+                    )
+                    .when_some(sort_icon, |this, icon| {
+                        this.child(Icon::new(icon).size(IconSize::XSmall).color(Color::Muted))
+                    })
+                    .when(self.sortable, |this| {
+                        let name = name.clone();
+                        this.cursor_pointer().on_click(cx.listener(
+                            move |this, _: &ClickEvent, _window, cx| {
+                                // Also keeps a double-click from triggering the Table's
+                                // built-in header width-reset.
+                                cx.stop_propagation();
+                                this.cycle_sort(name.clone(), cx);
+                            },
+                        ))
+                    })
+                    .into_any_element(),
+            );
+        }
+
+        let table = Table::new(cols)
+            .interactable(&self.interaction)
+            .striped()
+            .no_ui_font()
+            .disable_base_style()
+            .pin_cols(1)
+            .width_config(ColumnWidthConfig::Resizable(columns_state.clone()))
+            .header(header_cells)
+            .uniform_list(
+                "database-results",
+                row_count,
+                cx.processor(move |this, range: std::ops::Range<usize>, _window, cx| {
+                    range
+                        .filter_map(|row_ix| {
+                            let row = rows.get(row_ix)?;
+                            let mut cells: Vec<AnyElement> = Vec::with_capacity(cols);
+                            cells.push(
+                                div()
+                                    .h(row_height)
+                                    .w_full()
+                                    .px_1p5()
+                                    .text_right()
+                                    .text_color(text_muted)
+                                    .overflow_hidden()
+                                    .child(SharedString::from((row_ix + 1).to_string()))
+                                    .into_any_element(),
+                            );
+                            for (col_ix, cell) in row.iter().enumerate() {
+                                cells.push(this.render_cell(
+                                    row_ix, col_ix, cell, row_height, selected, &numeric, cx,
+                                ));
+                            }
+                            Some(cells)
+                        })
+                        .collect()
+                }),
+            )
+            .empty_table_callback(|_window, _cx| {
+                Label::new("No rows returned.")
+                    .size(LabelSize::Small)
+                    .color(Color::Muted)
+                    .into_any_element()
+            });
+
+        div()
+            .key_context("DatabaseResultsGrid")
+            .size_full()
+            .flex()
+            .flex_col()
+            .overflow_hidden()
+            .font_family(font_family)
+            .text_size(font_size)
+            .line_height(row_height)
+            .text_color(cx.theme().colors().text)
+            .on_action(cx.listener(|this, _: &CopyCellValue, _window, cx| {
+                this.copy_selected_cell(cx);
+            }))
+            .child(
+                div()
+                    .relative()
+                    .flex_1()
+                    .overflow_hidden()
+                    .child(
+                        // Muted strip behind the (transparent) header cells; the Table
+                        // itself draws the header's bottom border.
+                        div()
+                            .absolute()
+                            .top_0()
+                            .left_0()
+                            .right_0()
+                            .h(header_height)
+                            .bg(cx.theme().colors().surface_background),
+                    )
+                    .child(table),
+            )
+            .children(self.context_menu.as_ref().map(|(menu, position, _)| {
+                deferred(
+                    anchored()
+                        .position(*position)
+                        .anchor(gpui::Anchor::TopLeft)
+                        .child(menu.clone()),
+                )
+                .with_priority(1)
+            }))
+            .into_any_element()
+    }
+}
+
 /// The single source of the none→asc→desc→none sort-cycling decision.
 /// `cycle_sort` emits its output verbatim in `SortRequested`;
 /// QueryView (task 8) stores it verbatim — no duplicate cycle logic anywhere.
@@ -209,4 +622,40 @@ mod tests {
             Some(("name".into(), SortDirection::Ascending))
         );
     }
+
+    fn init_test(cx: &mut gpui::TestAppContext) {
+        cx.update(|cx| {
+            let settings_store = settings::SettingsStore::test(cx);
+            cx.set_global(settings_store);
+            theme_settings::init(theme::LoadThemes::JustBase, cx);
+        });
+    }
+
+    #[gpui::test]
+    fn set_result_builds_gutter_plus_data_column_state(cx: &mut gpui::TestAppContext) {
+        init_test(cx);
+        let grid = cx.new(ResultsGrid::new);
+        let result = QueryResult {
+            columns: vec!["id".into(), "name".into()],
+            rows: vec![vec![Some("1".into()), None]],
+            command_tag: None,
+            truncated: false,
+        };
+        grid.update(cx, |grid, cx| grid.set_result(Some(result), true, cx));
+        grid.read_with(cx, |grid, cx| {
+            let widths = grid
+                .columns_state
+                .as_ref()
+                .expect("columns state must exist once a result is set");
+            let widths = widths.read(cx);
+            assert_eq!(widths.cols(), 3, "gutter + 2 data columns");
+            assert_eq!(
+                widths.resize_behavior().as_slice()[0],
+                ui::TableResizeBehavior::None,
+                "gutter must not be resizable"
+            );
+            assert_eq!(grid.numeric_columns, vec![true, false]);
+            assert!(grid.sortable);
+        });
+    }
 }
