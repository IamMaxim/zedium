From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: IamMaxim <g.maxim.stepanoff@gmail.com>
Date: Sat, 27 Jun 2026 21:16:01 +0300
Subject: [PATCH] feat(db): connection store (json profiles + keychain
 passwords)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GbHBSdw2MBBgxKxLnVCbf6
---
 crates/database_client/Cargo.toml             |  1 +
 crates/database_client/src/connection.rs      |  1 +
 .../database_client/src/connection/store.rs   | 80 +++++++++++++++++++
 3 files changed, 82 insertions(+)
 create mode 100644 crates/database_client/src/connection/store.rs

diff --git a/crates/database_client/Cargo.toml b/crates/database_client/Cargo.toml
index 789378ac6d..7b3e4cf7b3 100644
--- a/crates/database_client/Cargo.toml
+++ b/crates/database_client/Cargo.toml
@@ -39,6 +39,7 @@ zed_actions.workspace = true
 
 [dev-dependencies]
 gpui = { workspace = true, features = ["test-support"] }
+tempfile.workspace = true
 
 [package.metadata.cargo-machete]
 ignored = ["log"]
diff --git a/crates/database_client/src/connection.rs b/crates/database_client/src/connection.rs
index 6b76aba686..f99742eec4 100644
--- a/crates/database_client/src/connection.rs
+++ b/crates/database_client/src/connection.rs
@@ -1 +1,2 @@
 pub mod profile;
+pub mod store;
diff --git a/crates/database_client/src/connection/store.rs b/crates/database_client/src/connection/store.rs
new file mode 100644
index 0000000000..9a8800677b
--- /dev/null
+++ b/crates/database_client/src/connection/store.rs
@@ -0,0 +1,80 @@
+use crate::connection::profile::ConnectionProfile;
+use anyhow::{Context as _, Result};
+use gpui::{App, AppContext as _, Task};
+use std::path::{Path, PathBuf};
+
+pub fn connections_path() -> PathBuf {
+    paths::config_dir().join("database_client").join("connections.json")
+}
+
+pub fn load_profiles() -> Vec<ConnectionProfile> {
+    load_profiles_from(&connections_path())
+}
+
+pub(crate) fn load_profiles_from(path: &Path) -> Vec<ConnectionProfile> {
+    match std::fs::read_to_string(path) {
+        Ok(text) => serde_json::from_str(&text).unwrap_or_else(|e| {
+            log::error!("failed to parse {path:?}: {e}");
+            Vec::new()
+        }),
+        Err(_) => Vec::new(),
+    }
+}
+
+pub fn save_profiles(profiles: &[ConnectionProfile]) -> Result<()> {
+    save_profiles_to(&connections_path(), profiles)
+}
+
+pub(crate) fn save_profiles_to(path: &Path, profiles: &[ConnectionProfile]) -> Result<()> {
+    if let Some(parent) = path.parent() {
+        std::fs::create_dir_all(parent).with_context(|| format!("creating {parent:?}"))?;
+    }
+    let json = serde_json::to_string_pretty(profiles)?;
+    std::fs::write(path, json).with_context(|| format!("writing {path:?}"))?;
+    Ok(())
+}
+
+pub fn store_password(cx: &App, profile: &ConnectionProfile, password: &str) -> Task<Result<()>> {
+    cx.write_credentials(&profile.keychain_url(), &profile.user, password.as_bytes())
+}
+
+pub fn read_password(cx: &App, profile: &ConnectionProfile) -> Task<Result<Option<String>>> {
+    let task = cx.read_credentials(&profile.keychain_url());
+    cx.background_spawn(async move {
+        match task.await? {
+            Some((_user, bytes)) => Ok(Some(String::from_utf8(bytes)?)),
+            None => Ok(None),
+        }
+    })
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+    use crate::connection::profile::SslMode;
+
+    fn sample() -> ConnectionProfile {
+        ConnectionProfile {
+            id: "p1".into(), name: "Local".into(), host: "localhost".into(),
+            port: 5432, database: "mydb".into(), user: "alice".into(),
+            ssl_mode: SslMode::Prefer,
+        }
+    }
+
+    #[test]
+    fn save_then_load_round_trips_via_explicit_path() {
+        let dir = tempfile::tempdir().unwrap();
+        let path = dir.path().join("connections.json");
+        let profiles = vec![sample()];
+        save_profiles_to(&path, &profiles).unwrap();
+        let loaded = load_profiles_from(&path);
+        assert_eq!(loaded, profiles);
+    }
+
+    #[test]
+    fn load_missing_file_returns_empty() {
+        let dir = tempfile::tempdir().unwrap();
+        let path = dir.path().join("nope.json");
+        assert!(load_profiles_from(&path).is_empty());
+    }
+}
