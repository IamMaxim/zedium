From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: IamMaxim <g.maxim.stepanoff@gmail.com>
Date: Sun, 31 May 2026 14:41:25 +0300
Subject: [PATCH] brand: user-facing brand + doc-link sweep (code)

Repoint user-facing documentation/help links from zed.dev to the fork
(docs -> https://iammaxim.github.io/zedium/docs, repo/menus ->
https://github.com/IamMaxim/zedium). The fork ships no docs site yet, so
these intentionally 404 until GitHub Pages is published.

- release_channel: app_id() dev.zed.Zed* -> dev.zedium.Zedium* (runtime
  Wayland/X11 WM_CLASS; must match the patch-0027 macOS bundle ids),
  app_identifier() Zed-Editor-* -> Zedium-Editor-*, docs base -> fork.
- zed_urls.rs: delete the dead account/start-trial/upgrade/terms/privacy/
  edit-prediction/skills helpers (sign-in + billing are gone); repoint the
  live rules/acp helpers to the fork and drop their unused cx/server_url.
- Delete the dead OpenAccountSettings action + handler.
- Remove the Zed-Pro upsell in the agent thread payment-required callout
  (now a provider-neutral "usage limit reached" message).
- Help menu: drop "Zed Twitter" / "Join the Team"; add "Zedium on GitHub".
- Repoint MCP OAuth CIMD_URL to the fork's client-metadata document.
- Neutralize zed.dev brand strings in ui component examples + a settings
  placeholder; repoint inherited doc-comment links.

Cloud-transport internals (http_client build_zed_*_url, the admin
impersonate call) and back-compat provider ids keep their zed.dev strings
and are handled via the verifier allowlist, not rebranded.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
---
 crates/agent_ui/src/agent_registry_ui.rs      |  2 +-
 .../src/conversation_view/thread_view.rs      | 17 +----
 crates/cli/src/main.rs                        |  2 +-
 crates/client/src/zed_urls.rs                 | 69 +++----------------
 crates/context_server/src/oauth.rs            |  4 +-
 crates/debugger_ui/src/debugger_panel.rs      |  4 +-
 .../src/edit_prediction_button.rs             |  2 +-
 crates/editor/src/edit_prediction.rs          |  2 +-
 crates/extensions_ui/src/extensions_ui.rs     | 38 +++++-----
 crates/language_onboarding/src/python.rs      |  2 +-
 crates/language_tools/src/key_context_view.rs |  2 +-
 crates/multi_buffer/src/multi_buffer.rs       |  2 +-
 crates/onboarding/src/multibuffer_hint.rs     |  2 +-
 crates/onboarding/src/onboarding.rs           |  1 +
 crates/project/src/project.rs                 |  2 +-
 .../tests/integration/project_tests.rs        |  2 +-
 crates/recent_projects/src/remote_servers.rs  |  2 +-
 crates/release_channel/src/lib.rs             | 34 ++++-----
 crates/remote/src/transport.rs                |  4 +-
 crates/remote/src/transport/ssh.rs            |  2 +-
 crates/repl/src/repl.rs                       |  2 +-
 crates/repl/src/repl_sessions_ui.rs           |  2 +-
 crates/settings/src/keymap_file.rs            |  2 +-
 crates/settings_ui/src/page_data.rs           |  2 +-
 .../telemetry_events/src/telemetry_events.rs  |  2 +-
 .../theme_selector/src/icon_theme_selector.rs |  2 +-
 crates/theme_selector/src/theme_selector.rs   |  2 +-
 .../ui/src/components/button/button_link.rs   |  2 +-
 .../components/collab/collab_notification.rs  |  2 +-
 .../src/components/list/list_bullet_item.rs   |  7 +-
 crates/zed/src/main.rs                        |  6 +-
 crates/zed/src/zed.rs                         | 29 +++-----
 crates/zed/src/zed/app_menus.rs               | 12 +---
 crates/zed/src/zed/open_listener.rs           |  4 --
 .../zed/src/zed/quick_action_bar/repl_menu.rs |  2 +-
 crates/zed_actions/src/lib.rs                 |  2 -
 36 files changed, 96 insertions(+), 179 deletions(-)

diff --git a/crates/agent_ui/src/agent_registry_ui.rs b/crates/agent_ui/src/agent_registry_ui.rs
index 897a853662..0112fcb79d 100644
--- a/crates/agent_ui/src/agent_registry_ui.rs
+++ b/crates/agent_ui/src/agent_registry_ui.rs
@@ -585,7 +585,7 @@ impl Render for AgentRegistryPage {
                                             .color(Color::Muted),
                                     )
                                     .on_click(move |_, _, cx| {
-                                        cx.open_url(&zed_urls::acp_registry_blog(cx))
+                                        cx.open_url(&zed_urls::acp_registry_blog())
                                     }),
                             ),
                     )
diff --git a/crates/agent_ui/src/conversation_view/thread_view.rs b/crates/agent_ui/src/conversation_view/thread_view.rs
index 9085b02470..253a55f8e0 100644
--- a/crates/agent_ui/src/conversation_view/thread_view.rs
+++ b/crates/agent_ui/src/conversation_view/thread_view.rs
@@ -9569,17 +9569,16 @@ impl ThreadView {
 
     fn render_payment_required_error(&self, cx: &mut Context<Self>) -> Callout {
         const ERROR_MESSAGE: &str =
-            "You reached your free usage limit. Upgrade to Zed Pro for more prompts.";
+            "Your model provider reported that you have reached a usage limit.";
 
         Callout::new()
             .severity(Severity::Error)
             .icon(IconName::XCircle)
-            .title("Free Usage Exceeded")
+            .title("Usage Limit Reached")
             .description(ERROR_MESSAGE)
             .actions_slot(
                 h_flex()
                     .gap_0p5()
-                    .child(self.upgrade_button(cx))
                     .child(self.create_copy_button(ERROR_MESSAGE)),
             )
             .dismiss_action(self.dismiss_error_button(cx))
@@ -9651,18 +9650,6 @@ impl ThreadView {
             }))
     }
 
-    fn upgrade_button(&self, cx: &mut Context<Self>) -> impl IntoElement {
-        Button::new("upgrade", "Upgrade")
-            .label_size(LabelSize::Small)
-            .style(ButtonStyle::Tinted(ui::TintColor::Accent))
-            .on_click(cx.listener({
-                move |this, _, _, cx| {
-                    this.clear_thread_error(cx);
-                    cx.open_url(&zed_urls::upgrade_to_zed_pro_url(cx));
-                }
-            }))
-    }
-
     fn authenticate_button(&self, cx: &mut Context<Self>) -> impl IntoElement {
         Button::new("authenticate", "Authenticate")
             .label_size(LabelSize::Small)
diff --git a/crates/cli/src/main.rs b/crates/cli/src/main.rs
index d261c99b02..cf391f735d 100644
--- a/crates/cli/src/main.rs
+++ b/crates/cli/src/main.rs
@@ -664,7 +664,7 @@ fn run() -> Result<()> {
 
     anyhow::ensure!(
         args.dev_server_token.is_none(),
-        "Dev servers were removed in v0.157.x please upgrade to SSH remoting: https://zed.dev/docs/remote-development"
+        "Dev servers were removed in v0.157.x please upgrade to SSH remoting: https://iammaxim.github.io/zedium/docs/remote-development"
     );
 
     rayon::ThreadPoolBuilder::new()
diff --git a/crates/client/src/zed_urls.rs b/crates/client/src/zed_urls.rs
index 94515d9283..be881baee8 100644
--- a/crates/client/src/zed_urls.rs
+++ b/crates/client/src/zed_urls.rs
@@ -1,69 +1,16 @@
-//! Contains helper functions for constructing URLs to various Zed-related pages.
-//!
-//! These URLs will adapt to the configured server URL in order to construct
-//! links appropriate for the environment (e.g., by linking to a local copy of
-//! zed.dev in development).
+//! Helper functions for constructing URLs to Zedium documentation and resources.
 
-use gpui::App;
-use settings::Settings;
-
-use crate::ClientSettings;
-
-fn server_url(cx: &App) -> &str {
-    &ClientSettings::get_global(cx).server_url
-}
-
-/// Returns the URL to the account page on zed.dev.
-pub fn account_url(cx: &App) -> String {
-    format!("{server_url}/account", server_url = server_url(cx))
-}
-
-/// Returns the URL to the start trial page on zed.dev.
-pub fn start_trial_url(cx: &App) -> String {
-    format!(
-        "{server_url}/account/start-trial",
-        server_url = server_url(cx)
-    )
-}
-
-/// Returns the URL to the upgrade page on zed.dev.
-pub fn upgrade_to_zed_pro_url(cx: &App) -> String {
-    format!("{server_url}/account/upgrade", server_url = server_url(cx))
-}
-
-/// Returns the URL to Zed's terms of service.
-pub fn terms_of_service(cx: &App) -> String {
-    format!("{server_url}/terms-of-service", server_url = server_url(cx))
-}
-
-/// Returns the URL to Zed AI's privacy and security docs.
-pub fn ai_privacy_and_security(cx: &App) -> String {
-    format!(
-        "{server_url}/docs/ai/privacy-and-security",
-        server_url = server_url(cx)
-    )
-}
-
-/// Returns the URL to Zed's edit prediction documentation.
-pub fn edit_prediction_docs(cx: &App) -> String {
-    format!(
-        "{server_url}/docs/ai/edit-prediction",
-        server_url = server_url(cx)
-    )
-}
-
-pub fn skills_docs(cx: &App) -> String {
-    format!("{server_url}/docs/ai/skills", server_url = server_url(cx))
+/// Returns the URL to Zedium's rules documentation.
+pub fn rules_docs() -> String {
+    "https://iammaxim.github.io/zedium/docs/ai/rules".to_string()
 }
 
-/// Returns the URL to Zed's ACP registry blog post.
-pub fn acp_registry_blog(cx: &App) -> String {
-    format!(
-        "{server_url}/blog/acp-registry",
-        server_url = server_url(cx)
-    )
+/// Returns the URL to Zedium's external-agent (ACP) documentation.
+pub fn acp_registry_blog() -> String {
+    "https://github.com/IamMaxim/zedium".to_string()
 }
 
+/// Returns the deep link to a shared agent thread.
 pub fn shared_agent_thread_url(session_id: &str) -> String {
     format!("zedium://agent/shared/{}", session_id)
 }
diff --git a/crates/context_server/src/oauth.rs b/crates/context_server/src/oauth.rs
index 7d50872900..96c0bfaf3f 100644
--- a/crates/context_server/src/oauth.rs
+++ b/crates/context_server/src/oauth.rs
@@ -33,8 +33,8 @@ use std::sync::Arc;
 use std::time::{Duration, SystemTime};
 use url::Url;
 
-/// The CIMD URL where Zed's OAuth client metadata document is hosted.
-pub const CIMD_URL: &str = "https://zed.dev/oauth/client-metadata.json";
+/// The CIMD URL where Zedium's OAuth client metadata document is hosted.
+pub const CIMD_URL: &str = "https://iammaxim.github.io/zedium/oauth/client-metadata.json";
 
 /// Validate that a URL is safe to use as an OAuth endpoint.
 ///
diff --git a/crates/debugger_ui/src/debugger_panel.rs b/crates/debugger_ui/src/debugger_panel.rs
index c034363bcd..07e7b24e40 100644
--- a/crates/debugger_ui/src/debugger_panel.rs
+++ b/crates/debugger_ui/src/debugger_panel.rs
@@ -647,7 +647,7 @@ impl DebugPanel {
         let documentation_button = || {
             IconButton::new("debug-open-documentation", IconName::CircleHelp)
                 .icon_size(IconSize::Small)
-                .on_click(move |_, _, cx| cx.open_url("https://zed.dev/docs/debugger"))
+                .on_click(move |_, _, cx| cx.open_url("https://iammaxim.github.io/zedium/docs/debugger"))
                 .tooltip(Tooltip::text("Open Documentation"))
         };
 
@@ -1851,7 +1851,7 @@ impl Render for DebugPanel {
                                         .size(IconSize::Small)
                                         .color(Color::Muted),
                                 )
-                                .on_click(|_, _, cx| cx.open_url("https://zed.dev/docs/debugger")),
+                                .on_click(|_, _, cx| cx.open_url("https://iammaxim.github.io/zedium/docs/debugger")),
                         )
                         .child(
                             Button::new(
diff --git a/crates/edit_prediction_ui/src/edit_prediction_button.rs b/crates/edit_prediction_ui/src/edit_prediction_button.rs
index 6ac80779eb..99227c1a60 100644
--- a/crates/edit_prediction_ui/src/edit_prediction_button.rs
+++ b/crates/edit_prediction_ui/src/edit_prediction_button.rs
@@ -45,7 +45,7 @@ actions!(
     ]
 );
 
-const PRIVACY_DOCS: &str = "https://zed.dev/docs/ai/privacy-and-security";
+const PRIVACY_DOCS: &str = "https://iammaxim.github.io/zedium/docs/ai/privacy-and-security";
 
 pub struct EditPredictionButton {
     editor_subscription: Option<(Subscription, usize)>,
diff --git a/crates/editor/src/edit_prediction.rs b/crates/editor/src/edit_prediction.rs
index 3ac38dd042..9807763fb1 100644
--- a/crates/editor/src/edit_prediction.rs
+++ b/crates/editor/src/edit_prediction.rs
@@ -2533,7 +2533,7 @@ impl Render for MissingEditPredictionKeybindingTooltip {
                             window.dispatch_action(zed_actions::OpenKeymapFile.boxed_clone(), cx)
                         }))
                         .child(Button::new("see-docs", "See Docs").size(ButtonSize::Compact).on_click(|_ev, _window, cx| {
-                            cx.open_url("https://zed.dev/docs/completions#edit-predictions-missing-keybinding");
+                            cx.open_url("https://iammaxim.github.io/zedium/docs/completions#edit-predictions-missing-keybinding");
                         })),
                 )
         })
diff --git a/crates/extensions_ui/src/extensions_ui.rs b/crates/extensions_ui/src/extensions_ui.rs
index 27672e6fd5..52dcdd2791 100644
--- a/crates/extensions_ui/src/extensions_ui.rs
+++ b/crates/extensions_ui/src/extensions_ui.rs
@@ -1445,7 +1445,7 @@ impl ExtensionsPage {
     }
 
     fn render_acp_registry_upsell(&self, cx: &mut Context<Self>) -> impl IntoElement {
-        let registry_url = zed_urls::acp_registry_blog(cx);
+        let registry_url = zed_urls::acp_registry_blog();
 
         let view_registry = Button::new("view_registry", "View Registry")
             .style(ButtonStyle::Tinted(ui::TintColor::Warning))
@@ -1557,110 +1557,110 @@ impl ExtensionsPage {
             let banner = match feature {
                 Feature::AgentClaude => self.render_feature_upsell_banner(
                     "Claude Agent support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/ai/external-agents#claude-agent".into(),
+                    "https://iammaxim.github.io/zedium/docs/ai/external-agents#claude-agent".into(),
                     false,
                     cx,
                 ),
                 Feature::AgentCodex => self.render_feature_upsell_banner(
                     "Codex CLI support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/ai/external-agents#codex-cli".into(),
+                    "https://iammaxim.github.io/zedium/docs/ai/external-agents#codex-cli".into(),
                     false,
                     cx,
                 ),
                 Feature::AgentGemini => self.render_feature_upsell_banner(
                     "Gemini CLI support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/ai/external-agents#gemini-cli".into(),
+                    "https://iammaxim.github.io/zedium/docs/ai/external-agents#gemini-cli".into(),
                     false,
                     cx,
                 ),
                 Feature::ExtensionBasedpyright => self.render_feature_upsell_banner(
                     "Basedpyright (Python language server) support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/python#basedpyright".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/python#basedpyright".into(),
                     false,
                     cx,
                 ),
                 Feature::ExtensionRuff => self.render_feature_upsell_banner(
                     "Ruff (linter for Python) support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/python#code-formatting--linting".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/python#code-formatting--linting".into(),
                     false,
                     cx,
                 ),
                 Feature::ExtensionTailwind => self.render_feature_upsell_banner(
                     "Tailwind CSS support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/tailwindcss".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/tailwindcss".into(),
                     false,
                     cx,
                 ),
                 Feature::ExtensionTy => self.render_feature_upsell_banner(
                     "Ty (Python language server) support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/python".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/python".into(),
                     false,
                     cx,
                 ),
                 Feature::Git => self.render_feature_upsell_banner(
                     "Zed comes with basic Git support—more features are coming in the future."
                         .into(),
-                    "https://zed.dev/docs/git".into(),
+                    "https://iammaxim.github.io/zedium/docs/git".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguageBash => self.render_feature_upsell_banner(
                     "Shell support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/bash".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/bash".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguageC => self.render_feature_upsell_banner(
                     "C support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/c".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/c".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguageCpp => self.render_feature_upsell_banner(
                     "C++ support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/cpp".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/cpp".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguageGo => self.render_feature_upsell_banner(
                     "Go support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/go".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/go".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguagePython => self.render_feature_upsell_banner(
                     "Python support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/python".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/python".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguageReact => self.render_feature_upsell_banner(
                     "React support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/typescript".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/typescript".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguageRust => self.render_feature_upsell_banner(
                     "Rust support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/rust".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/rust".into(),
                     false,
                     cx,
                 ),
                 Feature::LanguageTypescript => self.render_feature_upsell_banner(
                     "Typescript support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/languages/typescript".into(),
+                    "https://iammaxim.github.io/zedium/docs/languages/typescript".into(),
                     false,
                     cx,
                 ),
                 Feature::OpenIn => self.render_feature_upsell_banner(
                     "Zed supports linking to a source line on GitHub and others.".into(),
-                    "https://zed.dev/docs/git#git-integrations".into(),
+                    "https://iammaxim.github.io/zedium/docs/git#git-integrations".into(),
                     false,
                     cx,
                 ),
                 Feature::Vim => self.render_feature_upsell_banner(
                     "Vim support is built-in to Zed!".into(),
-                    "https://zed.dev/docs/vim".into(),
+                    "https://iammaxim.github.io/zedium/docs/vim".into(),
                     true,
                     cx,
                 ),
diff --git a/crates/language_onboarding/src/python.rs b/crates/language_onboarding/src/python.rs
index 64b6502327..547365d2c9 100644
--- a/crates/language_onboarding/src/python.rs
+++ b/crates/language_onboarding/src/python.rs
@@ -59,7 +59,7 @@ impl Render for BasedPyrightBanner {
                                         .label_size(LabelSize::Small)
                                         .end_icon(Icon::new(IconName::ArrowUpRight).size(IconSize::XSmall).color(Color::Muted))
                                         .on_click(|_, _, cx| {
-                                            cx.open_url("https://zed.dev/docs/languages/python")
+                                            cx.open_url("https://iammaxim.github.io/zedium/docs/languages/python")
                                         }),
                                 )
                                 .child(IconButton::new("dismiss", IconName::Close).icon_size(IconSize::Small).on_click(
diff --git a/crates/language_tools/src/key_context_view.rs b/crates/language_tools/src/key_context_view.rs
index d80840a640..eb6f6630c0 100644
--- a/crates/language_tools/src/key_context_view.rs
+++ b/crates/language_tools/src/key_context_view.rs
@@ -209,7 +209,7 @@ impl Render for KeyContextView {
                     .child(
                         Button::new("open_documentation", "Open Documentation")
                             .style(ButtonStyle::Filled)
-                            .on_click(|_, _, cx| cx.open_url("https://zed.dev/docs/key-bindings")),
+                            .on_click(|_, _, cx| cx.open_url("https://iammaxim.github.io/zedium/docs/key-bindings")),
                     )
                     .child(
                         Button::new("view_default_keymap", "View Default Keymap")
diff --git a/crates/multi_buffer/src/multi_buffer.rs b/crates/multi_buffer/src/multi_buffer.rs
index dd0c24fb5f..e07d6f5096 100644
--- a/crates/multi_buffer/src/multi_buffer.rs
+++ b/crates/multi_buffer/src/multi_buffer.rs
@@ -69,7 +69,7 @@ pub fn excerpt_context_lines(cx: &App) -> u32 {
 
 /// One or more [`Buffers`](Buffer) being edited in a single view.
 ///
-/// See <https://zed.dev/features#multi-buffers>
+/// See <https://iammaxim.github.io/zedium/docs/multibuffers>
 pub struct MultiBuffer {
     /// A snapshot of the [`Excerpt`]s in the MultiBuffer.
     /// Use [`MultiBuffer::snapshot`] to get a up-to-date snapshot.
diff --git a/crates/onboarding/src/multibuffer_hint.rs b/crates/onboarding/src/multibuffer_hint.rs
index 56092863c8..bd1e68b165 100644
--- a/crates/onboarding/src/multibuffer_hint.rs
+++ b/crates/onboarding/src/multibuffer_hint.rs
@@ -166,7 +166,7 @@ impl Render for MultibufferHint {
                                     .color(Color::Muted),
                             )
                             .on_click(move |_event, _, cx| {
-                                cx.open_url("https://zed.dev/docs/multibuffers")
+                                cx.open_url("https://iammaxim.github.io/zedium/docs/multibuffers")
                             }),
                     ),
             )
diff --git a/crates/onboarding/src/onboarding.rs b/crates/onboarding/src/onboarding.rs
index 17ce48c2d7..940080c9be 100644
--- a/crates/onboarding/src/onboarding.rs
+++ b/crates/onboarding/src/onboarding.rs
@@ -50,6 +50,7 @@ pub struct ImportCursorSettings {
 }
 
 pub const FIRST_OPEN: &str = "first_open";
+pub const DOCS_URL: &str = "https://iammaxim.github.io/zedium/docs/";
 
 actions!(
     onboarding,
diff --git a/crates/project/src/project.rs b/crates/project/src/project.rs
index 0783602e03..454e808526 100644
--- a/crates/project/src/project.rs
+++ b/crates/project/src/project.rs
@@ -3732,7 +3732,7 @@ impl Project {
                         notification_id: format!("local-tasks-{path:?}").into(),
                         link: Some(ToastLink {
                             label: "Open Tasks Documentation",
-                            url: "https://zed.dev/docs/tasks",
+                            url: "https://iammaxim.github.io/zedium/docs/tasks",
                         }),
                         message,
                     });
diff --git a/crates/project/tests/integration/project_tests.rs b/crates/project/tests/integration/project_tests.rs
index 674a2ace91..6177b9b265 100644
--- a/crates/project/tests/integration/project_tests.rs
+++ b/crates/project/tests/integration/project_tests.rs
@@ -1206,7 +1206,7 @@ async fn test_invalid_local_tasks_shows_toast_with_doc_link(cx: &mut gpui::TestA
             } => {
                 assert!(notification_id.starts_with("local-tasks-"));
                 assert!(message.contains("ZED_FOO"));
-                assert_eq!(*url, "https://zed.dev/docs/tasks");
+                assert_eq!(*url, "https://iammaxim.github.io/zedium/docs/tasks");
                 *saw_toast.borrow_mut() = true;
             }
             _ => {}
diff --git a/crates/recent_projects/src/remote_servers.rs b/crates/recent_projects/src/remote_servers.rs
index 05e2d76b53..ec8acd93f9 100644
--- a/crates/recent_projects/src/remote_servers.rs
+++ b/crates/recent_projects/src/remote_servers.rs
@@ -2348,7 +2348,7 @@ impl RemoteServerProjects {
                                             )
                                             .on_click(|_, _, cx| {
                                                 cx.open_url(
-                                                    "https://zed.dev/docs/remote-development",
+                                                    "https://iammaxim.github.io/zedium/docs/remote-development",
                                                 );
                                             }),
                                     ),
diff --git a/crates/release_channel/src/lib.rs b/crates/release_channel/src/lib.rs
index 493c02ef61..228f4112e2 100644
--- a/crates/release_channel/src/lib.rs
+++ b/crates/release_channel/src/lib.rs
@@ -7,7 +7,7 @@ use std::{env, str::FromStr, sync::LazyLock};
 use gpui::{App, Global};
 use semver::Version;
 
-const ZED_DOCS_URL: &str = "https://zed.dev/docs";
+const ZEDIUM_DOCS_URL: &str = "https://iammaxim.github.io/zedium/docs";
 
 /// stable | dev | nightly | preview
 pub static RELEASE_CHANNEL_NAME: LazyLock<String> = LazyLock::new(|| {
@@ -30,10 +30,10 @@ pub static RELEASE_CHANNEL: LazyLock<ReleaseChannel> =
 #[cfg(target_os = "windows")]
 pub fn app_identifier() -> &'static str {
     match *RELEASE_CHANNEL {
-        ReleaseChannel::Dev => "Zed-Editor-Dev",
-        ReleaseChannel::Nightly => "Zed-Editor-Nightly",
-        ReleaseChannel::Preview => "Zed-Editor-Preview",
-        ReleaseChannel::Stable => "Zed-Editor-Stable",
+        ReleaseChannel::Dev => "Zedium-Editor-Dev",
+        ReleaseChannel::Nightly => "Zedium-Editor-Nightly",
+        ReleaseChannel::Preview => "Zedium-Editor-Preview",
+        ReleaseChannel::Stable => "Zedium-Editor-Stable",
     }
 }
 
@@ -213,10 +213,10 @@ impl ReleaseChannel {
     /// This also has to match the bundle identifier for Zed on macOS.
     pub fn app_id(&self) -> &'static str {
         match self {
-            ReleaseChannel::Dev => "dev.zed.Zed-Dev",
-            ReleaseChannel::Nightly => "dev.zed.Zed-Nightly",
-            ReleaseChannel::Preview => "dev.zed.Zed-Preview",
-            ReleaseChannel::Stable => "dev.zed.Zed",
+            ReleaseChannel::Dev => "dev.zedium.Zedium-Dev",
+            ReleaseChannel::Nightly => "dev.zedium.Zedium-Nightly",
+            ReleaseChannel::Preview => "dev.zedium.Zedium-Preview",
+            ReleaseChannel::Stable => "dev.zedium.Zedium",
         }
     }
 
@@ -240,10 +240,10 @@ impl ReleaseChannel {
         };
 
         match channel_path_segment {
-            Some(channel) if slug.is_empty() => format!("{ZED_DOCS_URL}/{channel}"),
-            Some(channel) => format!("{ZED_DOCS_URL}/{channel}/{slug}"),
-            None if slug.is_empty() => ZED_DOCS_URL.to_string(),
-            None => format!("{ZED_DOCS_URL}/{slug}"),
+            Some(channel) if slug.is_empty() => format!("{ZEDIUM_DOCS_URL}/{channel}"),
+            Some(channel) => format!("{ZEDIUM_DOCS_URL}/{channel}/{slug}"),
+            None if slug.is_empty() => ZEDIUM_DOCS_URL.to_string(),
+            None => format!("{ZEDIUM_DOCS_URL}/{slug}"),
         }
     }
 }
@@ -274,19 +274,19 @@ mod tests {
     fn test_docs_url_for_release_channel() {
         assert_eq!(
             ReleaseChannel::Dev.docs_url("settings"),
-            "https://zed.dev/docs/nightly/settings"
+            "https://iammaxim.github.io/zedium/docs/nightly/settings"
         );
         assert_eq!(
             ReleaseChannel::Nightly.docs_url("settings"),
-            "https://zed.dev/docs/nightly/settings"
+            "https://iammaxim.github.io/zedium/docs/nightly/settings"
         );
         assert_eq!(
             ReleaseChannel::Preview.docs_url("settings"),
-            "https://zed.dev/docs/preview/settings"
+            "https://iammaxim.github.io/zedium/docs/preview/settings"
         );
         assert_eq!(
             ReleaseChannel::Stable.docs_url("settings"),
-            "https://zed.dev/docs/settings"
+            "https://iammaxim.github.io/zedium/docs/settings"
         );
     }
 }
diff --git a/crates/remote/src/transport.rs b/crates/remote/src/transport.rs
index 1794a68839..ee8e956892 100644
--- a/crates/remote/src/transport.rs
+++ b/crates/remote/src/transport.rs
@@ -33,7 +33,7 @@ fn parse_platform(output: &str) -> Result<RemotePlatform> {
         "Darwin" => RemoteOs::MacOs,
         "Linux" => RemoteOs::Linux,
         _ => anyhow::bail!(
-            "Prebuilt remote servers are not yet available for {os:?}. See https://zed.dev/docs/remote-development"
+            "Prebuilt remote servers are not yet available for {os:?}. See https://iammaxim.github.io/zedium/docs/remote-development"
         ),
     };
 
@@ -48,7 +48,7 @@ fn parse_platform(output: &str) -> Result<RemotePlatform> {
         RemoteArch::X86_64
     } else {
         anyhow::bail!(
-            "Prebuilt remote servers are not yet available for {arch:?}. See https://zed.dev/docs/remote-development"
+            "Prebuilt remote servers are not yet available for {arch:?}. See https://iammaxim.github.io/zedium/docs/remote-development"
         )
     };
 
diff --git a/crates/remote/src/transport/ssh.rs b/crates/remote/src/transport/ssh.rs
index 2beae54f2f..fd4941026e 100644
--- a/crates/remote/src/transport/ssh.rs
+++ b/crates/remote/src/transport/ssh.rs
@@ -1430,7 +1430,7 @@ impl SshSocket {
                 "AMD64" => RemoteArch::X86_64,
                 "ARM64" => RemoteArch::Aarch64,
                 arch => anyhow::bail!(
-                    "Prebuilt remote servers are not yet available for windows-{arch}. See https://zed.dev/docs/remote-development"
+                    "Prebuilt remote servers are not yet available for windows-{arch}. See https://iammaxim.github.io/zedium/docs/remote-development"
                 ),
             },
         })
diff --git a/crates/repl/src/repl.rs b/crates/repl/src/repl.rs
index 299641c47d..4edf5d14fd 100644
--- a/crates/repl/src/repl.rs
+++ b/crates/repl/src/repl.rs
@@ -26,7 +26,7 @@ pub use crate::repl_settings::ReplSettings;
 pub use crate::repl_store::ReplStore;
 pub use crate::session::Session;
 
-pub const KERNEL_DOCS_URL: &str = "https://zed.dev/docs/repl#changing-kernels";
+pub const KERNEL_DOCS_URL: &str = "https://iammaxim.github.io/zedium/docs/repl#changing-kernels";
 
 pub fn init(fs: Arc<dyn Fs>, cx: &mut App) {
     set_dispatcher(zed_dispatcher(cx));
diff --git a/crates/repl/src/repl_sessions_ui.rs b/crates/repl/src/repl_sessions_ui.rs
index 3bf242a186..2c9a7fb7ba 100644
--- a/crates/repl/src/repl_sessions_ui.rs
+++ b/crates/repl/src/repl_sessions_ui.rs
@@ -255,7 +255,7 @@ impl Render for ReplSessionsPage {
                             .child(Label::new("Install Kernels"))
                             .on_click(move |_, _, cx| {
                                 cx.open_url(
-                                    "https://zed.dev/docs/repl#language-specific-instructions",
+                                    "https://iammaxim.github.io/zedium/docs/repl#language-specific-instructions",
                                 )
                             }),
                     ),
diff --git a/crates/settings/src/keymap_file.rs b/crates/settings/src/keymap_file.rs
index 5e8d888f41..5d0bb7f065 100644
--- a/crates/settings/src/keymap_file.rs
+++ b/crates/settings/src/keymap_file.rs
@@ -66,7 +66,7 @@ pub struct KeymapSection {
     /// `Workspace`, the bindings will be active in that context. Boolean expressions like `X && Y`,
     /// `X || Y`, `!X` are also supported. Some more complex logic including checking OS and the
     /// current file extension are also supported - see [the
-    /// documentation](https://zed.dev/docs/key-bindings#contexts) for more details.
+    /// documentation](https://iammaxim.github.io/zedium/docs/key-bindings#contexts) for more details.
     #[serde(default)]
     pub context: String,
     /// This option enables specifying keys based on their position on a QWERTY keyboard, by using
diff --git a/crates/settings_ui/src/page_data.rs b/crates/settings_ui/src/page_data.rs
index 17a26b799d..e29a18e157 100644
--- a/crates/settings_ui/src/page_data.rs
+++ b/crates/settings_ui/src/page_data.rs
@@ -8285,7 +8285,7 @@ fn network_page() -> SettingsPage {
                     },
                 }),
                 metadata: Some(Box::new(SettingsFieldMetadata {
-                    placeholder: Some("https://zed.dev"),
+                    placeholder: Some("https://example.com"),
                     ..Default::default()
                 })),
                 files: USER,
diff --git a/crates/telemetry_events/src/telemetry_events.rs b/crates/telemetry_events/src/telemetry_events.rs
index e98a2c025f..152dc004a2 100644
--- a/crates/telemetry_events/src/telemetry_events.rs
+++ b/crates/telemetry_events/src/telemetry_events.rs
@@ -1,4 +1,4 @@
-//! See [Telemetry in Zed](https://zed.dev/docs/telemetry) for additional information.
+//! See [Telemetry in Zed](https://iammaxim.github.io/zedium/docs/telemetry) for additional information.
 
 use semver::Version;
 use serde::{Deserialize, Serialize};
diff --git a/crates/theme_selector/src/icon_theme_selector.rs b/crates/theme_selector/src/icon_theme_selector.rs
index 1a737be38a..d479743569 100644
--- a/crates/theme_selector/src/icon_theme_selector.rs
+++ b/crates/theme_selector/src/icon_theme_selector.rs
@@ -332,7 +332,7 @@ impl PickerDelegate for IconThemeSelectorDelegate {
                                 .color(Color::Muted),
                         )
                         .on_click(|_event, _window, cx| {
-                            cx.open_url("https://zed.dev/docs/icon-themes");
+                            cx.open_url("https://iammaxim.github.io/zedium/docs/icon-themes");
                         }),
                 )
                 .child(
diff --git a/crates/theme_selector/src/theme_selector.rs b/crates/theme_selector/src/theme_selector.rs
index bcf3790831..746c31c168 100644
--- a/crates/theme_selector/src/theme_selector.rs
+++ b/crates/theme_selector/src/theme_selector.rs
@@ -545,7 +545,7 @@ impl PickerDelegate for ThemeSelectorDelegate {
                                 .color(Color::Muted),
                         )
                         .on_click(cx.listener(|_, _, _, cx| {
-                            cx.open_url("https://zed.dev/docs/themes");
+                            cx.open_url("https://iammaxim.github.io/zedium/docs/themes");
                         })),
                 )
                 .child(
diff --git a/crates/ui/src/components/button/button_link.rs b/crates/ui/src/components/button/button_link.rs
index 841df96f55..ec71dbf4f6 100644
--- a/crates/ui/src/components/button/button_link.rs
+++ b/crates/ui/src/components/button/button_link.rs
@@ -91,7 +91,7 @@ impl Component for ButtonLink {
             .child(
                 example_group(vec![single_example(
                     "Simple",
-                    ButtonLink::new("zed.dev", "https://zed.dev").into_any_element(),
+                    ButtonLink::new("zedium", "https://github.com/IamMaxim/zedium").into_any_element(),
                 )])
                 .vertical(),
             )
diff --git a/crates/ui/src/components/collab/collab_notification.rs b/crates/ui/src/components/collab/collab_notification.rs
index 382749da37..4fbd58364c 100644
--- a/crates/ui/src/components/collab/collab_notification.rs
+++ b/crates/ui/src/components/collab/collab_notification.rs
@@ -126,7 +126,7 @@ impl Component for CollabNotification {
                             "a_very_long_username_that_might_overflow is sharing a project in Zed:",
                         ))
                         .child(
-                            Label::new("zed-cloud, zed, edit-prediction-bench, zed.dev")
+                            Label::new("project-alpha, backend, edit-prediction-bench, demo")
                                 .color(Color::Muted),
                         ),
                     )
diff --git a/crates/ui/src/components/list/list_bullet_item.rs b/crates/ui/src/components/list/list_bullet_item.rs
index 8fed315875..71973db691 100644
--- a/crates/ui/src/components/list/list_bullet_item.rs
+++ b/crates/ui/src/components/list/list_bullet_item.rs
@@ -99,8 +99,11 @@ impl Component for ListBulletItem {
             single_example(
                 "With Link",
                 ListBulletItem::new("")
-                    .child(Label::new("Create a Zed account by"))
-                    .child(ButtonLink::new("visiting the website", "https://zed.dev"))
+                    .child(Label::new("Learn more by"))
+                    .child(ButtonLink::new(
+                        "visiting the website",
+                        "https://github.com/IamMaxim/zedium",
+                    ))
                     .into_any_element(),
             ),
         ];
diff --git a/crates/zed/src/main.rs b/crates/zed/src/main.rs
index 0695ec9e8f..137384017e 100644
--- a/crates/zed/src/main.rs
+++ b/crates/zed/src/main.rs
@@ -156,7 +156,7 @@ fn fail_to_open_window_async(e: anyhow::Error, cx: &mut AsyncApp) {
 
 fn fail_to_open_window(e: anyhow::Error, _cx: &mut App) {
     eprintln!(
-        "Zed failed to open a window: {e:?}. See https://zed.dev/docs/linux for troubleshooting steps."
+        "Zed failed to open a window: {e:?}. See https://iammaxim.github.io/zedium/docs/linux for troubleshooting steps."
     );
     #[cfg(not(any(target_os = "linux", target_os = "freebsd")))]
     {
@@ -179,7 +179,7 @@ fn fail_to_open_window(e: anyhow::Error, _cx: &mut App) {
                     Notification::new("Zed failed to launch")
                         .body(Some(
                             format!(
-                                "{e:?}. See https://zed.dev/docs/linux for troubleshooting steps."
+                                "{e:?}. See https://iammaxim.github.io/zedium/docs/linux for troubleshooting steps."
                             )
                             .as_str(),
                         ))
@@ -1634,7 +1634,7 @@ struct Args {
     /// Use `path:line:row` syntax to open a file at a specific location.
     /// Non-existing paths and directories will ignore `:line:row` suffix.
     ///
-    /// URLs can either be `file://` or `zedium://` scheme, or relative to <https://zed.dev>.
+    /// URLs can either be `file://` or `zedium://` scheme, or relative to the configured server URL.
     paths_or_urls: Vec<String>,
 
     /// Pairs of file paths to diff. Can be specified multiple times.
diff --git a/crates/zed/src/zed.rs b/crates/zed/src/zed.rs
index 3a382c7974..e93df89bd5 100644
--- a/crates/zed/src/zed.rs
+++ b/crates/zed/src/zed.rs
@@ -21,7 +21,6 @@ pub use app_menus::*;
 use assets::Assets;
 
 use breadcrumbs::Breadcrumbs;
-use client::zed_urls;
 use collections::VecDeque;
 use debugger_ui::debugger_panel::DebugPanel;
 use editor::{Editor, MultiBuffer};
@@ -100,12 +99,10 @@ use workspace::{
 };
 use workspace::{Pane, notifications::DetachAndPromptErr};
 use zed_actions::{
-    About, OpenAccountSettings, OpenBrowser, OpenDocs, OpenServerSettings, OpenSettingsFile,
-    OpenStatusPage, OpenZedUrl, Quit,
+    About, OpenBrowser, OpenDocs, OpenServerSettings, OpenSettingsFile, OpenZedUrl, Quit,
 };
 
-const DOCS_URL: &str = "https://zed.dev/docs/";
-const STATUS_URL: &str = "https://status.zed.dev";
+const DOCS_URL: &str = "https://iammaxim.github.io/zedium/docs/";
 
 pub struct CrashHandler(pub Arc<crashes::Client>);
 
@@ -236,11 +233,6 @@ pub fn init(cx: &mut App) {
             );
         });
     })
-    .on_action(|_: &OpenAccountSettings, cx| {
-        with_active_or_new_workspace(cx, |_, _, cx| {
-            cx.open_url(&zed_urls::account_url(cx));
-        });
-    })
     .on_action(|_: &OpenTasks, cx| {
         with_active_or_new_workspace(cx, |_, window, cx| {
             open_settings_file(
@@ -627,7 +619,7 @@ fn initialize_file_watcher(window: &mut Window, cx: &mut Context<Workspace>) {
             db::indoc! {r#"
             inotify_init returned {}
 
-            This may be due to system-wide limits on inotify instances. For troubleshooting see: https://zed.dev/docs/linux
+            This may be due to system-wide limits on inotify instances. For troubleshooting see: https://iammaxim.github.io/zedium/docs/linux
             "#},
             e
         );
@@ -641,7 +633,7 @@ fn initialize_file_watcher(window: &mut Window, cx: &mut Context<Workspace>) {
         cx.spawn(async move |_, cx| {
             if prompt.await == Ok(0) {
                 cx.update(|cx| {
-                    cx.open_url("https://zed.dev/docs/linux#could-not-start-inotify");
+                    cx.open_url("https://iammaxim.github.io/zedium/docs/linux#could-not-start-inotify");
                     cx.quit();
                 });
             }
@@ -658,7 +650,7 @@ fn initialize_file_watcher(window: &mut Window, cx: &mut Context<Workspace>) {
             db::indoc! {r#"
             ReadDirectoryChangesW initialization failed: {}
 
-            This may occur on network filesystems and WSL paths. For troubleshooting see: https://zed.dev/docs/windows
+            This may occur on network filesystems and WSL paths. For troubleshooting see: https://iammaxim.github.io/zedium/docs/windows
             "#},
             e
         );
@@ -672,7 +664,7 @@ fn initialize_file_watcher(window: &mut Window, cx: &mut Context<Workspace>) {
         cx.spawn(async move |_, cx| {
             if prompt.await == Ok(0) {
                 cx.update(|cx| {
-                    cx.open_url("https://zed.dev/docs/windows");
+                    cx.open_url("https://iammaxim.github.io/zedium/docs/windows");
                     cx.quit()
                 });
             }
@@ -690,14 +682,14 @@ fn show_software_emulation_warning_if_needed(
         let (graphics_api, docs_url, open_url) = if cfg!(target_os = "windows") {
             (
                 "DirectX",
-                "https://zed.dev/docs/windows",
-                "https://zed.dev/docs/windows",
+                "https://iammaxim.github.io/zedium/docs/windows",
+                "https://iammaxim.github.io/zedium/docs/windows",
             )
         } else {
             (
                 "Vulkan",
-                "https://zed.dev/docs/linux",
-                "https://zed.dev/docs/linux#zed-fails-to-open-windows",
+                "https://iammaxim.github.io/zedium/docs/linux",
+                "https://iammaxim.github.io/zedium/docs/linux#zed-fails-to-open-windows",
             )
         };
         let message = format!(
@@ -869,7 +861,6 @@ fn register_actions(
 ) {
     workspace
         .register_action(|_, _: &OpenDocs, _, cx| cx.open_url(DOCS_URL))
-        .register_action(|_, _: &OpenStatusPage, _, cx| cx.open_url(STATUS_URL))
         .register_action(
             |workspace: &mut Workspace,
              _: &input_latency_ui::DumpInputLatencyHistogram,
diff --git a/crates/zed/src/zed/app_menus.rs b/crates/zed/src/zed/app_menus.rs
index c08e4d1acb..e5970bf6c1 100644
--- a/crates/zed/src/zed/app_menus.rs
+++ b/crates/zed/src/zed/app_menus.rs
@@ -288,19 +288,13 @@ pub fn app_menus(cx: &mut App) -> Vec<Menu> {
                 MenuItem::action(
                     "Documentation",
                     super::OpenBrowser {
-                        url: "https://zed.dev/docs".into(),
+                        url: "https://iammaxim.github.io/zedium/docs".into(),
                     },
                 ),
                 MenuItem::action(
-                    "Zed Twitter",
+                    "Zedium on GitHub",
                     super::OpenBrowser {
-                        url: "https://twitter.com/zeddotdev".into(),
-                    },
-                ),
-                MenuItem::action(
-                    "Join the Team",
-                    super::OpenBrowser {
-                        url: "https://zed.dev/jobs".into(),
+                        url: "https://github.com/IamMaxim/zedium".into(),
                     },
                 ),
             ],
diff --git a/crates/zed/src/zed/open_listener.rs b/crates/zed/src/zed/open_listener.rs
index 2b858cf838..b06211ac8a 100644
--- a/crates/zed/src/zed/open_listener.rs
+++ b/crates/zed/src/zed/open_listener.rs
@@ -186,13 +186,9 @@ impl OpenRequest {
                 } else {
                     log::error!("Invalid session ID in URL: {}", session_id_str);
                 }
-<<<<<<< HEAD
             } else if url.starts_with(agent_skills::SKILL_SHARE_LINK_PREFIX) {
                 this.parse_skill_install_url(&url)?
-            } else if let Some(agent_path) = url.strip_prefix("zed://agent") {
-=======
             } else if let Some(agent_path) = url.strip_prefix("zedium://agent") {
->>>>>>> c05db00b9d (brand: rename zed:// URL scheme to zedium://)
                 this.parse_agent_url(agent_path)
             } else if url == "zedium://" || url == "zedium://open" || url == "zedium://open/" {
                 this.kind = Some(OpenRequestKind::FocusApp);
diff --git a/crates/zed/src/zed/quick_action_bar/repl_menu.rs b/crates/zed/src/zed/quick_action_bar/repl_menu.rs
index 7d8b6ae769..03a6c36a62 100644
--- a/crates/zed/src/zed/quick_action_bar/repl_menu.rs
+++ b/crates/zed/src/zed/quick_action_bar/repl_menu.rs
@@ -16,7 +16,7 @@ use util::ResultExt;
 
 use super::QuickActionBar;
 
-const ZED_REPL_DOCUMENTATION: &str = "https://zed.dev/docs/repl";
+const ZED_REPL_DOCUMENTATION: &str = "https://iammaxim.github.io/zedium/docs/repl";
 
 struct ReplMenuState {
     tooltip: SharedString,
diff --git a/crates/zed_actions/src/lib.rs b/crates/zed_actions/src/lib.rs
index 0ad33cf924..ba9e1a52e3 100644
--- a/crates/zed_actions/src/lib.rs
+++ b/crates/zed_actions/src/lib.rs
@@ -55,8 +55,6 @@ actions!(
         /// Opens the keymap editor.
         #[action(deprecated_aliases = ["zed_actions::OpenKeymapEditor"])]
         OpenKeymap,
-        /// Opens account settings.
-        OpenAccountSettings,
         /// Opens server settings.
         OpenServerSettings,
         /// Quits the application.
