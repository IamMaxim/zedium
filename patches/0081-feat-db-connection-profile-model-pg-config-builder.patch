From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: IamMaxim <g.maxim.stepanoff@gmail.com>
Date: Sat, 27 Jun 2026 21:12:35 +0300
Subject: [PATCH] feat(db): connection profile model + pg config builder

---
 crates/database_client/src/connection.rs      |  1 +
 .../database_client/src/connection/profile.rs | 93 +++++++++++++++++++
 crates/database_client/src/database_client.rs |  2 +
 3 files changed, 96 insertions(+)
 create mode 100644 crates/database_client/src/connection.rs
 create mode 100644 crates/database_client/src/connection/profile.rs

diff --git a/crates/database_client/src/connection.rs b/crates/database_client/src/connection.rs
new file mode 100644
index 0000000000..6b76aba686
--- /dev/null
+++ b/crates/database_client/src/connection.rs
@@ -0,0 +1 @@
+pub mod profile;
diff --git a/crates/database_client/src/connection/profile.rs b/crates/database_client/src/connection/profile.rs
new file mode 100644
index 0000000000..c618ea37a1
--- /dev/null
+++ b/crates/database_client/src/connection/profile.rs
@@ -0,0 +1,93 @@
+use serde::{Deserialize, Serialize};
+
+#[derive(Debug, Clone, Copy, PartialEq, Eq, Default, Serialize, Deserialize)]
+#[serde(rename_all = "lowercase")]
+pub enum SslMode {
+    Disable,
+    #[default]
+    Prefer,
+    Require,
+}
+
+#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
+pub struct ConnectionProfile {
+    pub id: String,
+    pub name: String,
+    pub host: String,
+    pub port: u16,
+    pub database: String,
+    pub user: String,
+    #[serde(default)]
+    pub ssl_mode: SslMode,
+}
+
+impl ConnectionProfile {
+    /// Stable key used to store/retrieve the password in the OS keychain.
+    pub fn keychain_url(&self) -> String {
+        format!(
+            "postgres://{}@{}:{}/{}",
+            self.user, self.host, self.port, self.database
+        )
+    }
+
+    /// Build a tokio-postgres connection config. TLS negotiation is decided by
+    /// the caller based on `ssl_mode`; this only fills the connection fields.
+    pub fn pg_config(&self, password: Option<&str>) -> tokio_postgres::Config {
+        let mut cfg = tokio_postgres::Config::new();
+        cfg.host(&self.host)
+            .port(self.port)
+            .dbname(&self.database)
+            .user(&self.user)
+            .application_name("Zedium");
+        if let Some(pw) = password {
+            cfg.password(pw);
+        }
+        cfg
+    }
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    fn sample() -> ConnectionProfile {
+        ConnectionProfile {
+            id: "p1".into(),
+            name: "Local".into(),
+            host: "localhost".into(),
+            port: 5432,
+            database: "mydb".into(),
+            user: "alice".into(),
+            ssl_mode: SslMode::Prefer,
+        }
+    }
+
+    #[test]
+    fn serde_round_trip() {
+        let p = sample();
+        let json = serde_json::to_string(&p).unwrap();
+        let back: ConnectionProfile = serde_json::from_str(&json).unwrap();
+        assert_eq!(p, back);
+    }
+
+    #[test]
+    fn ssl_mode_serializes_lowercase() {
+        assert_eq!(serde_json::to_string(&SslMode::Require).unwrap(), "\"require\"");
+    }
+
+    #[test]
+    fn keychain_url_is_stable_and_unique() {
+        let p = sample();
+        assert_eq!(p.keychain_url(), "postgres://alice@localhost:5432/mydb");
+    }
+
+    #[test]
+    fn pg_config_sets_fields() {
+        let p = sample();
+        let cfg = p.pg_config(Some("secret"));
+        assert_eq!(cfg.get_hosts().len(), 1);
+        assert_eq!(cfg.get_ports(), &[5432]);
+        assert_eq!(cfg.get_dbname(), Some("mydb"));
+        assert_eq!(cfg.get_user(), Some("alice"));
+    }
+}
diff --git a/crates/database_client/src/database_client.rs b/crates/database_client/src/database_client.rs
index c5fc627dc8..1314e7f323 100644
--- a/crates/database_client/src/database_client.rs
+++ b/crates/database_client/src/database_client.rs
@@ -2,6 +2,8 @@
 
 use gpui::App;
 
+mod connection;
+
 /// Initialize the database client: register actions, panel, and items.
 pub fn init(_cx: &mut App) {
     // Wiring added in later tasks.
